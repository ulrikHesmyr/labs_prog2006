//stack = [List(...), Code("10 *")]



[ 1 2 3 ] map { 10 * }

"map" => {
    let code_block = tokens.pop().unwrap();
    let code = code_block.split(' ').collect();
    let list = stack.pop().unwrap();
    list.map(i => exec(format!("{} {} * ", code_block[0], i, code_block[1])))
}
